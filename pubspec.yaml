name: weathermate
description: WeatherMate Flutter application
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  provider: ^6.1.2
  dio: ^5.4.3
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  uuid: ^4.4.0
  fl_chart: ^0.66.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

